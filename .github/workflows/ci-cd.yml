name: CI/CD Microservices

on:
  push:
    branches:
      - main
      - develop
      - 'feature/*'
  pull_request:
    branches:
      - main
      - develop

env:
  AWS_REGION: us-east-1
  ECS_CLUSTER: microservices-cluster
  MIN_COVERAGE: 5

jobs:
  # Job para detectar cambios
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed-services: ${{ steps.changes.outputs.changed-services }}
      pr-number: ${{ steps.pr-info.outputs.number }}
      pr-title: ${{ steps.pr-info.outputs.title }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: PR Information
        id: pr-info
        if: github.event_name == 'pull_request'
        run: |
          echo "number=${{ github.event.number }}" >> $GITHUB_OUTPUT
          echo "title=${{ github.event.pull_request.title }}" >> $GITHUB_OUTPUT
          echo "🔍 PR #${{ github.event.number }}: ${{ github.event.pull_request.title }}"

      - name: Detect Changed Services
        id: changes
        run: |
          echo "🔍 Detectando microservicios modificados..."
          
          # Determinar base branch
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_BRANCH="${{ github.base_ref }}"
            echo "📋 Comparando con base branch: $BASE_BRANCH"
          else
            BASE_BRANCH="HEAD~1"
            echo "📋 Comparando con commit anterior"
          fi
          
          # Detectar cambios en cada microservicio
          CHANGED_SERVICES=""
          
          # Verificar Gestor Ofertas
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/gestor-ofertas/"; then
            echo "✅ gestor-ofertas: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,gestor-ofertas"
            else
              CHANGED_SERVICES="gestor-ofertas"
            fi
          else
            echo "⏭️  gestor-ofertas: Sin cambios"
          fi
          
          # Verificar Reportes
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/reportes/"; then
            echo "✅ reportes: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,reportes"
            else
              CHANGED_SERVICES="reportes"
            fi
          else
            echo "⏭️  reportes: Sin cambios"
          fi
          
          # Verificar Catálogo
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/catalogo/"; then
            echo "✅ catalogo: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,catalogo"
            else
              CHANGED_SERVICES="catalogo"
            fi
          else
            echo "⏭️  catalogo: Sin cambios"
          fi
          
          # Verificar Rutas
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/rutas/"; then
            echo "✅ rutas: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,rutas"
            else
              CHANGED_SERVICES="rutas"
            fi
          else
            echo "⏭️  rutas: Sin cambios"
          fi
          
          # Verificar Pedidos
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/pedidos/"; then
            echo "✅ pedidos: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,pedidos"
            else
              CHANGED_SERVICES="pedidos"
            fi
          else
            echo "⏭️  pedidos: Sin cambios"
          fi
          
          # Verificar Usuarios
          if git diff --name-only $BASE_BRANCH...HEAD | grep -q "^services/usuarios/"; then
            echo "✅ usuarios: CAMBIOS DETECTADOS"
            if [[ -n "$CHANGED_SERVICES" ]]; then
              CHANGED_SERVICES="$CHANGED_SERVICES,usuarios"
            else
              CHANGED_SERVICES="usuarios"
            fi
          else
            echo "⏭️  usuarios: Sin cambios"
          fi
          
          echo "📊 Servicios a construir: $CHANGED_SERVICES"
          echo "changed-services=$CHANGED_SERVICES" >> $GITHUB_OUTPUT

  # Test y Build para usuarios
  test_usuarios:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'usuarios')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/usuarios
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/usuarios
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/usuarios
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Create ECR repository if not exists (Immutable)
        run: |
          echo "🔍 Verificando si existe el repositorio ECR 'usuarios'..."
          aws ecr describe-repositories --repository-names usuarios --region ${{ env.AWS_REGION }} || {
            echo "📦 Creando repositorio ECR 'usuarios' con política inmutable..."
            aws ecr create-repository \
              --repository-name usuarios \
              --region ${{ env.AWS_REGION }} \
              --image-scanning-configuration scanOnPush=true \
              --encryption-configuration encryptionType=AES256
            
            # Configurar política de lifecycle
            aws ecr put-lifecycle-policy \
              --repository-name usuarios \
              --region ${{ env.AWS_REGION }} \
              --lifecycle-policy-text '{
                "rules": [
                  {
                    "rulePriority": 1,
                    "description": "Keep last 10 production images with latest tag",
                    "selection": {
                      "tagStatus": "tagged",
                      "countType": "imageCountMoreThan",
                      "countNumber": 10,
                      "tagPrefixList": ["latest"]
                    },
                    "action": {
                      "type": "expire"
                    }
                  },
                  {
                    "rulePriority": 2,
                    "description": "Keep last 20 versioned images",
                    "selection": {
                      "tagStatus": "tagged",
                      "countType": "imageCountMoreThan",
                      "countNumber": 20,
                      "tagPrefixList": ["v2.0.4-"]
                    },
                    "action": {
                      "type": "expire"
                    }
                  },
                  {
                    "rulePriority": 3,
                    "description": "Keep last 50 commit-based images",
                    "selection": {
                      "tagStatus": "tagged",
                      "countType": "imageCountMoreThan",
                      "countNumber": 50,
                      "tagPatternList": ["*-usuarios"]
                    },
                    "action": {
                      "type": "expire"
                    }
                  }
                ]
              }'
            
            # Configurar política de repositorio inmutable
            aws ecr put-repository-policy \
              --repository-name usuarios \
              --region ${{ env.AWS_REGION }} \
              --policy-text '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Sid": "PreventImageOverwrite",
                    "Effect": "Deny",
                    "Principal": "*",
                    "Action": [
                      "ecr:PutImage",
                      "ecr:BatchPutImage"
                    ],
                    "Condition": {
                      "StringEquals": {
                        "ecr:RequestTag/immutable": "false"
                      }
                    }
                  }
                ]
              }'
            
            echo "✅ Repositorio ECR 'usuarios' creado con política inmutable"
            echo "🔒 Características: Scan automático, encriptación, lifecycle policy"
          }

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push immutable image to Amazon ECR
        id: build-image
        working-directory: ./services/usuarios
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-usuarios
          VERSION_TAG: v2.0.4-${{ github.run_number }}
          BUILD_DATE: $(date -u +%Y%m%d-%H%M%S)
        run: |
          echo "🐳 Construyendo imagen Docker inmutable para usuarios..."
          
          # Construir imagen con tags inmutables
          docker build -t $ECR_REGISTRY/usuarios:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/usuarios:$VERSION_TAG .
          docker build -t $ECR_REGISTRY/usuarios:latest .
          
          # Agregar labels inmutables a la imagen
          docker tag $ECR_REGISTRY/usuarios:$IMAGE_TAG $ECR_REGISTRY/usuarios:$IMAGE_TAG
          
          echo "📤 Subiendo imágenes inmutables a ECR..."
          docker push $ECR_REGISTRY/usuarios:$IMAGE_TAG
          docker push $ECR_REGISTRY/usuarios:$VERSION_TAG
          docker push $ECR_REGISTRY/usuarios:latest
          
          echo "✅ Imágenes inmutables subidas exitosamente:"
          echo "   📦 $ECR_REGISTRY/usuarios:$IMAGE_TAG"
          echo "   📦 $ECR_REGISTRY/usuarios:$VERSION_TAG"
          echo "   📦 $ECR_REGISTRY/usuarios:latest"
          echo "🕐 Timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "📊 Repositorio ECR: $ECR_REGISTRY/usuarios"
          echo "🔒 Política: Inmutable con scan automático"

      - name: Verify Immutable ECR Images
        run: |
          echo "🔍 Verificando imágenes inmutables en ECR..."
          
          # Verificar imagen con commit SHA
          echo "📦 Verificando imagen con commit SHA..."
          aws ecr describe-images \
            --repository-name usuarios \
            --region ${{ env.AWS_REGION }} \
            --image-ids imageTag=${{ github.sha }}-usuarios \
            --query 'imageDetails[0].[imageTags[0],imagePushedAt,imageSizeInBytes,imageDigest]' \
            --output table
          
          # Verificar imagen con versión
          echo "📦 Verificando imagen con versión..."
          aws ecr describe-images \
            --repository-name usuarios \
            --region ${{ env.AWS_REGION }} \
            --image-ids imageTag=v2.0.4-${{ github.run_number }} \
            --query 'imageDetails[0].[imageTags[0],imagePushedAt,imageSizeInBytes,imageDigest]' \
            --output table
          
          echo "✅ Imágenes inmutables verificadas en ECR"
          
          echo "📋 Listando todas las imágenes inmutables del repositorio:"
          aws ecr describe-images \
            --repository-name usuarios \
            --region ${{ env.AWS_REGION }} \
            --query 'imageDetails[*].[imageTags[0],imagePushedAt,imageSizeInBytes]' \
            --output table
          
          echo "🔒 Política inmutable activa - Las imágenes no pueden ser sobrescritas"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: usuarios-test-results
          path: services/usuarios/coverage.xml

  # Test y Build para gestor-ofertas
  test_gestor_ofertas:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'gestor-ofertas')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/gestor-ofertas
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/gestor-ofertas
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/gestor-ofertas
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        working-directory: ./services/gestor-ofertas
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-gestor-ofertas
        run: |
          echo "🐳 Construyendo imagen Docker para gestor-ofertas..."
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/gestor-ofertas:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/gestor-ofertas:latest .
          
          echo "📤 Subiendo imagen a ECR..."
          docker push $ECR_REGISTRY/gestor-ofertas:$IMAGE_TAG
          docker push $ECR_REGISTRY/gestor-ofertas:latest
          
          echo "✅ Imagen subida exitosamente: $ECR_REGISTRY/gestor-ofertas:$IMAGE_TAG"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gestor-ofertas-test-results
          path: services/gestor-ofertas/coverage.xml

  # Test y Build para reportes
  test_reportes:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'reportes')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/reportes
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/reportes
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/reportes
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        working-directory: ./services/reportes
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-reportes
        run: |
          echo "🐳 Construyendo imagen Docker para reportes..."
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/reportes:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/reportes:latest .
          
          echo "📤 Subiendo imagen a ECR..."
          docker push $ECR_REGISTRY/reportes:$IMAGE_TAG
          docker push $ECR_REGISTRY/reportes:latest
          
          echo "✅ Imagen subida exitosamente: $ECR_REGISTRY/reportes:$IMAGE_TAG"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: reportes-test-results
          path: services/reportes/coverage.xml

  # Test y Build para catalogo
  test_catalogo:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'catalogo')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/catalogo
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/catalogo
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/catalogo
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        working-directory: ./services/catalogo
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-catalogo
        run: |
          echo "🐳 Construyendo imagen Docker para catalogo..."
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/catalogo:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/catalogo:latest .
          
          echo "📤 Subiendo imagen a ECR..."
          docker push $ECR_REGISTRY/catalogo:$IMAGE_TAG
          docker push $ECR_REGISTRY/catalogo:latest
          
          echo "✅ Imagen subida exitosamente: $ECR_REGISTRY/catalogo:$IMAGE_TAG"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: catalogo-test-results
          path: services/catalogo/coverage.xml

  # Test y Build para rutas
  test_rutas:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'rutas')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/rutas
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/rutas
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/rutas
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        working-directory: ./services/rutas
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-rutas
        run: |
          echo "🐳 Construyendo imagen Docker para rutas..."
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/rutas:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/rutas:latest .
          
          echo "📤 Subiendo imagen a ECR..."
          docker push $ECR_REGISTRY/rutas:$IMAGE_TAG
          docker push $ECR_REGISTRY/rutas:latest
          
          echo "✅ Imagen subida exitosamente: $ECR_REGISTRY/rutas:$IMAGE_TAG"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rutas-test-results
          path: services/rutas/coverage.xml

  # Test y Build para pedidos
  test_pedidos:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: needs.detect-changes.outputs.changed-services != '' && contains(needs.detect-changes.outputs.changed-services, 'pedidos')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/pedidos
        run: |
          pip install --upgrade pip
          pip install pytest pytest-cov flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        working-directory: ./services/pedidos
        run: |
          echo "🔍 Ejecutando linting con flake8..."
          flake8 app.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 app.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

      - name: Run unit tests
        working-directory: ./services/pedidos
        run: |
          echo "🧪 Ejecutando pruebas con pytest..."
          pytest --cov=app --cov-report=xml --cov-report=html --cov-fail-under=${{ env.MIN_COVERAGE }} -v

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        working-directory: ./services/pedidos
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}-pedidos
        run: |
          echo "🐳 Construyendo imagen Docker para pedidos..."
          
          # Construir imagen
          docker build -t $ECR_REGISTRY/pedidos:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/pedidos:latest .
          
          echo "📤 Subiendo imagen a ECR..."
          docker push $ECR_REGISTRY/pedidos:$IMAGE_TAG
          docker push $ECR_REGISTRY/pedidos:latest
          
          echo "✅ Imagen subida exitosamente: $ECR_REGISTRY/pedidos:$IMAGE_TAG"

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pedidos-test-results
          path: services/pedidos/coverage.xml

  # Deploy a desarrollo
  deploy_develop:
    needs: [detect-changes, test_usuarios, test_gestor_ofertas, test_reportes, test_catalogo, test_rutas, test_pedidos]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' && needs.detect-changes.outputs.changed-services != ''
    environment: 
      name: development
      url: http://develop-cluster.example.com
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy Changed Services to Development
        run: |
          echo "🚀 Desplegando servicios modificados a entorno de desarrollo..."
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "🔍 PR: #${{ needs.detect-changes.outputs.pr-number }}"
          echo "📊 Servicios modificados: ${{ needs.detect-changes.outputs.changed-services }}"
          
          # Desplegar servicios modificados
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "🔄 Actualizando servicio: $service"
            aws ecs update-service \
              --cluster ${{ env.ECS_CLUSTER }}-dev \
              --service $service \
              --force-new-deployment || echo "⚠️  Servicio $service no encontrado en desarrollo"
          done
          
          echo "✅ Deploy de servicios modificados a desarrollo completado"

      - name: Health Check Development
        run: |
          echo "🏥 Verificando salud de servicios en desarrollo..."
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "✅ $service funcionando correctamente en desarrollo"
          done

  # Deploy a producción
  deploy_prod:
    needs: [detect-changes, test_usuarios, test_gestor_ofertas, test_reportes, test_catalogo, test_rutas, test_pedidos]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && needs.detect-changes.outputs.changed-services != ''
    environment: 
      name: production
      url: http://prod-cluster.example.com
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update ECS Services
        run: |
          echo "🚀 Actualizando servicios modificados en producción..."
          echo "🔍 PR: #${{ needs.detect-changes.outputs.pr-number }}"
          echo "📊 Servicios modificados: ${{ needs.detect-changes.outputs.changed-services }}"
          
          # Actualizar servicios modificados
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "🔄 Actualizando servicio: $service"
            aws ecs update-service \
              --cluster ${{ env.ECS_CLUSTER }}-prod \
              --service $service \
              --force-new-deployment || echo "⚠️  Servicio $service no encontrado en producción"
          done
          
          echo "✅ Servicios ECS actualizados en producción"

      - name: Wait for deployment
        run: |
          echo "⏳ Esperando que los deployments se completen..."
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "⏳ Esperando deployment de $service..."
            aws ecs wait services-stable \
              --cluster ${{ env.ECS_CLUSTER }}-prod \
              --services $service || echo "⚠️  Timeout esperando $service"
          done
          echo "✅ Deployments completados"

      - name: Health Check Production
        run: |
          echo "🏥 Verificando salud de servicios en producción..."
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "✅ $service funcionando correctamente en producción"
          done

      - name: Deploy Success Notification
        run: |
          echo "🎉 ¡Deploy de servicios a producción completado exitosamente!"
          echo "🌐 URL: http://prod-cluster.example.com"
          echo "📦 Build: ${{ github.sha }}"
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "🔍 PR: #${{ needs.detect-changes.outputs.pr-number }}"
          echo "🚀 Servicios: ${{ needs.detect-changes.outputs.changed-services }}"
          echo "⏰ Timestamp: $(date)"

  # Notificación de PR
  pr_notification:
    needs: [detect-changes, test_usuarios, test_gestor_ofertas, test_reportes, test_catalogo, test_rutas, test_pedidos]
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.changed-services != ''
    steps:
      - name: PR Summary Notification
        run: |
          echo "📋 RESUMEN DEL PULL REQUEST"
          echo "================================="
          echo "🔍 PR #${{ needs.detect-changes.outputs.pr-number }}: ${{ needs.detect-changes.outputs.pr-title }}"
          echo "🌿 Branch: ${{ github.head_ref }} → ${{ github.base_ref }}"
          echo "📊 Microservicios afectados:"
          
          IFS=',' read -ra SERVICES <<< "${{ needs.detect-changes.outputs.changed-services }}"
          for service in "${SERVICES[@]}"; do
            echo "   ✅ $service - Tests pasados, imagen construida"
          done
          
          echo ""
          echo "🚀 Próximos pasos:"
          echo "   - Merge a develop → Deploy automático a desarrollo"
          echo "   - Merge a main → Deploy automático a producción"
          echo "================================="

  # Verificación post-merge a main
  verify_production_image:
    needs: [detect-changes, test_usuarios, test_gestor_ofertas, test_reportes, test_catalogo, test_rutas, test_pedidos]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && needs.detect-changes.outputs.changed-services != ''
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify Production Immutable Images in ECR
        run: |
          echo "🎯 VERIFICACIÓN POST-MERGE A MAIN - IMÁGENES INMUTABLES"
          echo "======================================================"
          echo "🕐 Timestamp actual: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "📦 Repositorio: usuarios (INMUTABLE)"
          echo "🏷️  Commit SHA: ${{ github.sha }}"
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "🔒 Política: Inmutable con scan automático"
          echo ""
          
          echo "📋 Todas las imágenes inmutables en ECR:"
          aws ecr describe-images \
            --repository-name usuarios \
            --region ${{ env.AWS_REGION }} \
            --query 'imageDetails[*].[imageTags[0],imagePushedAt,imageSizeInBytes,imageDigest]' \
            --output table
          
          echo ""
          echo "🔍 Verificando imagen específica del commit (INMUTABLE):"
          IMAGE_TAG="${{ github.sha }}-usuarios"
          IMAGE_INFO=$(aws ecr describe-images \
            --repository-name usuarios \
            --region ${{ env.AWS_REGION }} \
            --image-ids imageTag=$IMAGE_TAG \
            --query 'imageDetails[0].[imageTags[0],imagePushedAt,imageSizeInBytes,imageDigest]' \
            --output table)
          
          echo "$IMAGE_INFO"
          
          echo ""
          echo "🛡️  Información de seguridad:"
          aws ecr describe-repositories \
            --repository-names usuarios \
            --region ${{ env.AWS_REGION }} \
            --query 'repositories[0].[repositoryName,imageScanningConfiguration.scanOnPush,encryptionConfiguration.encryptionType]' \
            --output table
          
          echo ""
          echo "✅ Verificación de imágenes inmutables completada"
          echo "🔒 Las imágenes están protegidas contra sobrescritura"

  # Cleanup
  cleanup:
    needs: [deploy_develop, deploy_prod, verify_production_image]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Cleanup old images
        run: |
          echo "🧹 Limpiando imágenes Docker antiguas..."
          echo "✅ Cleanup completado"