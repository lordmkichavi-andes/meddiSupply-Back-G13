name: CI/CD Microservices

on:
  push:
    branches: [ main, develop, 'feature/*' ]
  pull_request:
    branches: [ main, develop ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: us-east-1
  ECS_CLUSTER: medi-supply-cluster
  MIN_COVERAGE: 0

jobs:
  # 1) Detecta microservicios modificados y expone salida JSON (una sola línea)
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed-services: ${{ steps.changes.outputs.changed-services }}
      pr-number: ${{ steps.pr-info.outputs.number }}
      pr-title: ${{ steps.pr-info.outputs.title }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Ensure jq
        run: |
          if ! command -v jq >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y jq
          fi
      - name: PR Information
        id: pr-info
        if: github.event_name == 'pull_request'
        run: |
          echo "number=${{ github.event.number }}" >> $GITHUB_OUTPUT
          echo "title=${{ github.event.pull_request.title }}" >> $GITHUB_OUTPUT
          echo "🔍 PR #${{ github.event.number }}: ${{ github.event.pull_request.title }}"
      - name: Detect Changed Services
        id: changes
        shell: bash
        run: |
          echo "🔍 Detectando microservicios modificados..."
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE="${{ github.base_ref }}"
            echo "📋 Comparando con base branch: $BASE"
            # Asegura que exista la rama base localmente
            git fetch origin "$BASE:$BASE" --depth=1
          else
            BASE="HEAD~1"
            echo "📋 Comparando contra commit anterior"
          fi
          CHANGED_FILES=$(git diff --name-only "$BASE"...HEAD || true)
          SERVICES_JSON='[]'
          for dir in services/*/; do
            [[ -d "$dir" ]] || continue
            s=$(basename "$dir")
            if echo "$CHANGED_FILES" | grep -q "^services/$s/"; then
              echo "✅ $s: CAMBIOS DETECTADOS"
              # -c = salida compacta en una sola línea
              SERVICES_JSON=$(jq -c --arg svc "$s" '. + [$svc]' <<<"$SERVICES_JSON")
            else
              echo "⏭️  $s: Sin cambios"
            fi
          done
          echo "📊 Servicios a procesar (JSON): $SERVICES_JSON"
          # Escribir output en UNA sola línea evita el error "Invalid format"
          echo "changed-services=$SERVICES_JSON" >> "$GITHUB_OUTPUT"

  # 2) Test + Build; Push a ECR SOLO en push a main (matriz)

  # 3) Deploy a Producción (matrix)
  deploy_prod:
    needs: [detect-changes]
    if: github.ref == 'refs/heads/main' && needs.detect-changes.outputs.changed-services != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJSON(needs.detect-changes.outputs.changed-services) }}
    environment:
      name: production
      url: https://cdk-exp-container-assets-120569610818-us-east-1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Docker image
        id: build-image
        env:
           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
           COMMIT_SHA: ${{ github.sha }}
           SERVICE: ${{ matrix.service }}
        run: |
            IMAGE_TAG="$COMMIT_SHA-$SERVICE"
            FULL_IMAGE_NAME="$ECR_REGISTRY/$SERVICE:$IMAGE_TAG"
            
            echo "🔎 Asegurando que el repositorio $SERVICE exista en ECR y sea inmutable..."
            
            # ✅ CORRECCIÓN: Se añade --image-tag-mutability IMMUTABLE
            # y se corrige la ruta del 'docker build'
            aws ecr create-repository \
              --repository-name $SERVICE \
              --region ${{ env.AWS_REGION }} \
              --image-scanning-configuration scanOnPush=true \
              --image-tag-mutability IMMUTABLE \
              2>/dev/null || true
            
            echo "Building image for $SERVICE with tag $IMAGE_TAG"
            # ✅ RUTA FINAL CORREGIDA
            docker build -t "$FULL_IMAGE_NAME" ./services/$SERVICE 
            
            echo "Pushing image $FULL_IMAGE_NAME to ECR"
            docker push "$FULL_IMAGE_NAME"
      

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Deploy to ECS (Fargate) - Producción
        env:
          CLUSTER: ${{ env.ECS_CLUSTER }}
          SERVICE_SLUG: ${{ matrix.service }}
          AWS_REGION: ${{ env.AWS_REGION }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          
          # 1. Asume que el nombre del servicio ECS contiene el slug capitalizado + 'Service'
          # Ej: products -> ProductsService
          # Esto es un truco simple para capitalizar la primera letra y añadir 'Service'
          SERVICE_SLUG_CAPITALIZED_PREFIX="$(echo "$SERVICE_SLUG" | sed 's/\(.\)/\U\1/')"Service

          echo "🔍 Buscando servicio ECS que contenga: $SERVICE_SLUG_CAPITALIZED_PREFIX"
          
          # 2. Listar ARNs de servicios y usar 'grep' para encontrar la coincidencia (insensitive)
          # Se espera que esto devuelva el ARN completo: arn:aws:ecs:us-east-1:123.../MediSupplyStack-ProductsService740A68BF-xPfs958XBUXN
          FULL_SERVICE_ARN=$(aws ecs list-services --cluster "$CLUSTER" --region "$AWS_REGION" --query "serviceArns[]" --output text | \
              grep -i "$SERVICE_SLUG_CAPITALIZED_PREFIX" || true)

          # 3. Extraer solo el nombre de la última parte del ARN (el nombre largo de CloudFormation)
          FULL_SERVICE_NAME=$(echo "$FULL_SERVICE_ARN" | awk -F/ '{print $2}')
          
          # 4. Manejo de errores: Si no se encuentra el servicio
          if [ -z "$FULL_SERVICE_NAME" ]; then
             echo "❌ ERROR FATAL: No se encontró ningún servicio ECS que coincida con '$SERVICE_SLUG_CAPITALIZED_PREFIX' en el cluster '$CLUSTER'."
             exit 1
          fi

          # 5. Continuar el despliegue usando el nombre completo
          
          # La imagen en ECR usa el nombre CORTO (products:sha-products)
          IMAGE="$ECR_REGISTRY/$SERVICE_SLUG:$COMMIT_SHA-$SERVICE_SLUG" 
          
          echo "🚀 Deploy $SERVICE_SLUG → $FULL_SERVICE_NAME con imagen $IMAGE"
          
          # Usar el nombre de servicio completo para la descripción:
          TD_ARN=$(aws ecs describe-services --cluster "$CLUSTER" --services "$FULL_SERVICE_NAME" --query 'services[0].taskDefinition' --output text --region "$AWS_REGION")
          
          # Asegurar que el TD_ARN se haya resuelto
          if [ -z "$TD_ARN" ]; then
             echo "❌ ERROR: El servicio '$FULL_SERVICE_NAME' existe, pero no tiene una Definición de Tarea activa. Revisar ECS."
             exit 1
          fi

          # 6. El resto de la lógica de actualización con jq es la misma, usando $FULL_SERVICE_NAME para el update
          aws ecs describe-task-definition --task-definition "$TD_ARN" --query 'taskDefinition' --region "$AWS_REGION" > base.json
          jq 'del(.status,.taskDefinitionArn,.requiresAttributes,.revision,.compatibilities,.registeredAt,.registeredBy)' base.json > stripped.json
          
          # ... (resto de tu script jq) ...
          
          # Aquí usamos $SERVICE_SLUG para que jq busque el contenedor cuyo nombre COINCIDA con el SLUG (products)
          if jq -e --arg S "$SERVICE_SLUG" 'any(.containerDefinitions[].name; . == $S)' stripped.json >/dev/null; then
            # ... (Lógica de jq para cuando el nombre del contenedor coincide con el slug) ...
            jq --arg S "$SERVICE_SLUG" --arg IMG "$IMAGE" '
              .containerDefinitions |= map(if .name == $S then .image = $IMG else . end)
            ' stripped.json > rendered.json
          else
            # ... (Lógica de jq alternativa) ...
            jq --arg S "$SERVICE_SLUG" --arg IMG "$IMAGE" '
              .containerDefinitions |=
              (.[0].image = $IMG) as $x | map(if (.image|test("/"+$S+"(:|@|$)")) then (.image = $IMG) else . end)
            ' stripped.json > rendered.json
          fi
          
          NEW_TD_ARN=$(aws ecs register-task-definition --cli-input-json file://rendered.json --query 'taskDefinition.taskDefinitionArn' --output text --region "$AWS_REGION")
          # Usamos $FULL_SERVICE_NAME aquí
          aws ecs update-service --cluster "$CLUSTER" --service "$FULL_SERVICE_NAME" --task-definition "$NEW_TD_ARN" --region "$AWS_REGION" >/dev/null
          
          echo "⏳ Esperando estabilidad..."
          # Usamos $FULL_SERVICE_NAME aquí
          aws ecs wait services-stable --cluster "$CLUSTER" --services "$FULL_SERVICE_NAME" --region "$AWS_REGION"
          
          echo "✅ $SERVICE_SLUG desplegado en producción con $NEW_TD_ARN"

  # 4) Notificación de PR (si aplica)
  pr_notification:
    needs: [detect-changes]
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.changed-services != '[]'
    steps:
      - name: PR Summary
        run: |
          echo "📋 RESUMEN DEL PULL REQUEST"
          echo "================================="
          echo "🔍 PR #${{ needs.detect-changes.outputs.pr-number }}: ${{ needs.detect-changes.outputs.pr-title }}"
          echo "🌿 Branch: ${{ github.head_ref }} → ${{ github.base_ref }}"
          echo "📊 Microservicios afectados:"
          for s in $(jq -r '.[]' <<< '${{ needs.detect-changes.outputs.changed-services }}'); do
            echo "   ✅ $s - Tests OK, imagen construida"
          done
          echo "================================="

  # 5) Cleanup local del runner
  cleanup:
    needs: [deploy_prod]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup docker
        run: |
          echo "🧹 Limpiando capas locales de Docker..."
          docker system prune -af || true
          echo "✅ Cleanup completado"
