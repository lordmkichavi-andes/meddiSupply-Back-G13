version: '3.8'

services:
  # 🗄️ Servicio de Base de Datos PostgreSQL
  db:
    image: postgres:14-alpine
    container_name: postgres_rdwo
    restart: always
    environment:
      POSTGRES_DB: medisupplydb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"  # Puerto externo 5434 → interno 5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  orders-service:
    image: 120569610818.dkr.ecr.us-east-1.amazonaws.com/cdk-exp-container-assets-120569610818-us-east-1:d45971b679dbd5b43fc38113e9d43dc32c622071-orders
    container_name: orders_service
    platform: linux/amd64
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    environment:
        DB_HOST: db
        DB_PORT: 5432
        DB_NAME: medisupplydb
        DB_USER: postgres
        DB_PASSWORD: postgres
        RUN_DB_INIT_ON_STARTUP: True
    command: ["gunicorn", "--bind", "0.0.0.0:8080", "app:create_app()"]